{
  "name": "renew-simulator-gateway",
  "version": "1.0.0",
  "description": "Gateway server for connecting external simulators to renew-lib",
  "main": "src/main.js",
  "scripts": {
    "start": "node .",
    "dev": "nodemon .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "socket.io": "^2.2.0"
  },
  "devDependencies": {
    "nodemon": "^1.19.1"
  }
}
